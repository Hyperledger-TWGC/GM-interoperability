package workshop_test

import (
	. "github.com/onsi/ginkgo"
	//. "github.com/onsi/gomega"
)

var _ = Describe("Crypto", func() {
	// generate keypair file via tj or ccs
	It("it should able to generate keypair and store in file", func() {

	})
	// general function for degist hash, sign
	It("it should able to complete degist via sm3 and sign", func() {

	})
	// general function for degist hash, verify
	It("it should able to complete degist and verify", func() {

	})
	// general function for encrypt
	It("it should able to complete sm2 encrypt", func() {

	})
	// general function for decrypt
	It("it should able to complete sm2 decrypt", func() {

	})
	// general function for sm4 encrypt
	It("it should able to complete sm4 encrypt", func() {

	})
	// general function for sm4 decrypt
	It("it should able to complete sm4 decrypt", func() {

	})
})
