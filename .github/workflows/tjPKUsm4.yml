name: tjPKUsm4test
on:
  pull_request:
  push:
  schedule:
    - cron: '0 0 * * 3' # https://crontab.guru/#0_0_*_*_3
jobs:
  tjPKUsm4test:
    uses: ./.github/workflows/basicCIsm4.yml
    with:
      SOURCE: TJ
      TARGET: PKU